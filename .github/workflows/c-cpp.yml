name: 테스트 실행

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - "**"

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    
    - name: Checkout code
    - uses: actions/checkout@v2

    - name: make
      run: make
